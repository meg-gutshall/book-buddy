<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Author:</strong>
  <%= @book.author %>
</p>

<p>
  <strong>Genre:</strong>
  <%= @book.genre %>
</p>

<p>
  <strong>Copies:</strong>
  <%= @book.copies %>
</p>

<%= link_to 'New Borrow', new_borrow_path %> | 
<%= link_to 'New Hold', new_hold_path %><br><br>
<%= hide_admin_action(link_to "Borrows History", book_borrows_path(@book)) %><%= hide_admin_action(" | ") %>
<%= hide_admin_action(link_to "Holds History", book_holds_path(@book)) %>
<%= hide_admin_action(tag.br) %><%= hide_admin_action(tag.br) %>
<%= hide_admin_action(link_to "Edit", edit_book_path(@book)) %><%= hide_admin_action(" | ") %>
<%= link_to "Back", books_path %> | 
<%= link_to "Home", root_path %>
