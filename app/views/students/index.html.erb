<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th>School Name</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.school.name %></td>
        <td><%= student.first_name %></td>
        <td><%= student.last_name %></td>
        <td><%= student.email %></td>
        <td><% if current_student %><%= link_to "Show", student %><% end %><%= hide_admin_action(link_to "Show", student_admin_registration_path(current_admin, student)) %></td>
        <td><%= link_to "Edit", edit_student_admin_registration_path(current_admin, student) %></td>
        <td><%= link_to "Delete", student_admin_registration_path(current_admin, student), method: :delete, data: { confirm: "Are you sure?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= hide_admin_action(link_to "New Student", new_student_admin_registration_path(current_admin)) %>
<%= link_to "Home", root_path %>
